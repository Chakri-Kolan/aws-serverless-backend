# Architecture

Client requests flow through API Gateway and invoke Lambda functions.

Responses are persisted and queried from DynamoDB.

Designed for horizontal scalability and low operational overhead.
